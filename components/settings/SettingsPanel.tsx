'use client'

/**
 * @deprecated Settings have moved to /settings?tab=appearance.
 * This stub is kept for backwards compatibility — it renders nothing.
 */
export function SettingsPanel() {
  return null
}

export default SettingsPanel
